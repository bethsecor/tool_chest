<!-- Move to index page -->
<!-- Link to tools page -->
<div id="tools">
  <ul>
    <% @user.tools.each do |tool| %>
      <li><%= tool.name %></li>
    <% end %>
  </ul>
</div>


<%= link_to "Logout", logout_path, method: :delete %>
